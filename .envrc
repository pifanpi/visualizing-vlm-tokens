# .envrc file

# Path to your conda installation
export CONDA_BASE_PATH="$HOME/miniconda3"  # Adjust this path as needed

# Initialize conda
eval "$($CONDA_BASE_PATH/bin/conda shell.bash hook)"

# Now we can activate the environment
conda activate vlmtokens

